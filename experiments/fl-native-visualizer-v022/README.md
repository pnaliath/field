# Field V0.22 — stable room and stereo

Targets from V0.21 diagnostics/user test:

- pull the useful Z range forward; no back-wall pile-up
- max two spatial voices, never three
- split only on strong repeated L/R evidence and keep the split stable once learned
- longer visual presence hold/release to reduce flicker between hits
- preserve V0.19 response fixes and V0.21 browser-style width baseline
- retain diagnostics for validation
