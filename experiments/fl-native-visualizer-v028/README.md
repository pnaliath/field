# Field V0.28 — Readable EQ Delta

Experimental FL-native build. Stable learned body geometry remains the baseline; the current FFT frame is compared against that baseline to exaggerate and highlight local EQ boosts/cuts. Defaults: X sensitivity 0.95x, Z sensitivity 1.20x, disappear time 500 ms.
